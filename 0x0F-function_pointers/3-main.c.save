#include <stdio.h>
#include <stdlib.h>
#include "calc.h"

/**
 * main - test operational functions
 * argv: array of arguments
 * argc: size of array of arguments
 *
 * Return: result of the operation
 */

int main(char **argv, int argc)
{

	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}

	if ((argv[2][0] != "+" &&) || if (argc != 4)
        {
        printf("Error\n");                                                                 exit(98);                                                                          } )
	{
		printf("Error\n");
		exit(98);
	}
}
